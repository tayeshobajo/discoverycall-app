import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import './globals.css';

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
});

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
});

export const metadata: Metadata = {
  title: 'DiscoveryCall — AI-powered sales discovery agents',
  description: 'Embed an AI discovery agent on your website. SPIN + MEDDIC + Challenger frameworks baked in. Built by Trust Tai.',
  metadataBase: new URL(process.env.NEXT_PUBLIC_APP_URL ?? 'https://app.discoverycall.ai'),
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
